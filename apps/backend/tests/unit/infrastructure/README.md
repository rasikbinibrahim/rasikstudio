# apps/backend/tests/unit/infrastructure/

Unit tests for infrastructure implementations that can be tested without real external services. Tests that require real PostgreSQL or Redis belong in `tests/integration/`.
