# apps/desktop/tests/unit/features/

Unit tests for feature components and feature-local hooks. Mirrors `src/features/` structure.

Each feature subdirectory tests only that feature's components. Cross-feature state integration is tested in E2E tests.
