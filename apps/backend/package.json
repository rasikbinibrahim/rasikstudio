{
  "name": "@rasik-studio/backend",
  "version": "0.1.0",
  "private": true,
  "description": "Rasik Studio backend service (FastAPI) — Turborepo script shim over uv",
  "scripts": {
    "dev": "uv run uvicorn app.main:app --reload --host 127.0.0.1 --port 8000",
    "build": "uv sync",
    "lint": "echo 'no backend lint configured yet'"
  }
}
